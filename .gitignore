__pycache__/
*.pyc
.DS_Store
*.aux
*.bbl
*.blg
*.log
*.out
*.fdb_latexmk
*.fls
*.synctex.gz
paper/reference/
paper/*.tex
paper/*.pdf
paper/*.zip
experiments/ms/
SUBMISSION_STATUS.md

# results-cli ledger: hashes stay local, not committed
.results/

# Private correspondence with the journal: cover letters, reviewer reports,
# response letters, and anything built from them. Public repo.
paper/submission/cover_letter/
reviews/RESPONSE_TO_REVIEWERS*
reviews/REVIEWER*_AUDIT*
reviews/patches/patch_letter_*
reviews/patches/build_audit_*
reviews/mobile/
SUBMISSIONS/

# Derived: audit caches and verifier output, regenerable from the scripts
reviews/verification/output/

# The packet README is correspondence: it summarizes the revision for the
# reviewers. The builder script beside it is tooling and stays tracked.
reviews/packet/README.md

# Submission materials — never tracked (private journal correspondence)
SUBMISSION_STATUS.md
SUBMISSION.md
SUBMISSION_TODO.md
submission/
submissions/
cover_letter*
cover-letter*
COVER_LETTER*
fee_waiver*
fee-waiver*
*_submission.zip

# Round-3 Perplexity review packet: carries reviewer reports verbatim
reviews/round3_packet/
